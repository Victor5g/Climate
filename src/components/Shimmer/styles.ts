import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  defaultHeightShimmer: {
    height: 40,
  },

  containerShimmer: {
    marginBottom: 24,
    opacity: 0.5,
    borderRadius: 30,
    top: 8,
  },
});
